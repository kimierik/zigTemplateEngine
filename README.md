# testing a html templating engine in zig

this is a hobby project and not mean to be used in any production

# useage
run template tests
```sh
zig build test
```

start a small (currently unsafe) server that returns templates
```sh
zig build run
```
